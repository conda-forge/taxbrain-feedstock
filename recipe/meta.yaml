{% set name = "taxbrain" %}
{% set version = "2.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PSLmodels/Tax-Brain/archive/refs/tags/{{ version }}.tar.gz
  sha256: 79d78cdf7a26afdd6d3183f5850d4455531cea3a98d5ced1c07c4ea69ff828f5

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  entry_points:
    - taxbrain = taxbrain.cli:cli_main

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - taxcalc >=3.0.0
    - behresp >=0.11.0
    - dask
    - bokeh
    - markdown
    - tabulate
    - pypandoc
    - matplotlib-base
    - numpy >=1.14

test:
  imports:
    - taxbrain

about:
  home: https://github.com/PSLmodels/Tax-Brain
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: 'A Python package that integrates tax models under a single, simple interface'
  description: Tax-Brain is an integrator model for PSL tax models
  doc_url: http://taxbrain.pslmodels.org/content/intro.html
  dev_url: https://github.com/PSLmodels/Tax-Brain

extra:
  recipe-maintainers:
    - andersonfrailey
